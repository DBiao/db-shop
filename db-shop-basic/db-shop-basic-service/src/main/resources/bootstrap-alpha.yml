spring:
  cloud:
    nacos:
      username: nacos
      password: nacos
      config:
        server-addr: "192.168.1.212:8848"
        namespace: "clip2-${spring.profiles.active:}"
logging:
  file:
    path: "./logs/"